import SignIn from '../../components/signIn/SignIn';

import './Authenticate.scss';

const Authenticate = () => {
  return (
    <div>
      <SignIn />
    </div>
  );
};

export default Authenticate;
